# 满浆率计算使用手册

## 快速流程

1. 在左侧“上传图像或 ZIP”一次选择一张、多张图像或 ZIP 压缩包。
2. 系统自动建立图像队列；在纵向浏览器中点击缩略图切换当前预览。
3. 默认使用“自动 / Otsu 自动阈值”，无需手工设置参数。
4. 检查当前图的原图、前景掩码和绿色叠加预览。
5. 点击“分析整个队列”，统一处理队列中全部图像。
6. 下载 CSV、结果 ZIP；两张及以上图像还可查看 3D 前景命中高程。

单张和多张图像共用同一入口、同一预览和同一结果流程，不再切换输入模式。

## 自动算法

### Otsu 自动阈值

自动根据整张图像的灰度分布计算阈值。速度快，适合光照、背景和材料颜色比较均匀的图像，是默认方案。

### 多段式自动分割

来自 `experiments/multisegment_benchmark.py` 的实验方案，处理步骤为：

1. 校正大尺度光照变化。
2. 根据材料与光照残差建立最多 6 个不规则分区。
3. 每个分区独立计算 Otsu 阈值。
4. 合并各分区前景并进行形态学后处理。

它适合探索明显阴影或局部亮度差异场景。现有评测没有人工标注真值，因此正式质量判定前仍需用已标注样本验证。

## 高级算法

- **全局阈值**：所有像素使用同一个手工灰度阈值。
- **自适应阈值**：按局部窗口计算阈值，可调整窗口尺寸和偏移量 C。
- **Otsu 自动阈值**：保留自动阈值，同时允许调整后处理参数。
- **多段式分割**：允许逐区设置阈值并保存参数标准。

高级模式下可以调整平滑核尺寸和最小前景连通区，减少细碎噪点。

## 多段式参数标准

多段式分区按光照残差由暗到亮编号，而不是简单按左、右、上、下编号。相同拍摄条件下，这使分区参数更容易迁移到其他图像。

### 固定阈值

每个分区直接使用保存的绝对阈值。它强调同一套判定标准，适合采集条件稳定的批处理。

### 相对自动阈值偏移

每张图先独立计算各分区自动阈值，再叠加保存的正负偏移。它在保持统一调节倾向的同时，对光照变化更有容忍度。

操作方式：

1. 选择一张代表性图像作为当前参考图。
2. 点击“用当前图自动值填充”。
3. 逐区调整阈值并观察掩码、叠加图和分区前景率。
4. 输入参数标准名称，点击“保存/更新参数标准”。
5. 后续可以从左侧加载已保存标准，或下载 JSON 留档。

整队分析会保存一份 `analysis_parameters.json`，保证结果可以追溯到当时使用的算法和参数。

## 3D 前景命中高程

完成至少两张图像的整队分析后，可打开“查看 3D 前景命中高程”。

- 高程：该像素位置被多少张图判定为前景。
- 颜色：该位置的前景命中率。
- 俯视热力图：用于快速定位稳定覆盖区和低命中区。

不同尺寸图像会转换成归一化坐标后统计。要使结果具有业务意义，多张图应保持方向、拍摄角度和裁剪边界一致。该视图反映多图空间一致性，不等同于物理厚度。

## 材料对比

- **黑胶白砖**：较暗区域按胶层前景处理。
- **白胶黑砖**：较亮区域按胶层前景处理。

无论选择哪种材料对比，最终掩码中的白色像素都代表计入满浆率的前景区域。

## 拍摄建议

- 镜头与砖面保持平行，避免透视变形。
- 只保留砖面，尽量去掉背景和夹具。
- 光线明亮、均匀，避免硬阴影和强反光。
- 批量与 3D 分析时保持裁剪范围、方向和分辨率一致。
- 先用代表性样本校准参数，再应用到整个队列。

## 输出文件

- CSV：每张图的状态、满浆率和算法。
- 结果图：原图与前景掩码并排展示。
- 叠加图：绿色显示被判定为前景的区域。
- Mask：单通道 PNG，白色为前景。
- 参数 JSON：记录本次批量分析使用的完整参数。

服务器输出保存在 `temp/batch_时间戳/`，重新构建 Docker 容器不会删除 `temp/` 中的历史结果。
